// ---- mock the API service to avoid pulling ESM libs from node_modules ----
const makeRequest = jest.fn(); // exportable if you want to assert later

jest.mock('../../services/useApiService', () => ({
  __esModule: true,
  default: () => ({ makeRequest }),
}));

// (optional) if some other code still touches wmf/react-library directly:
jest.mock('wmf/react-library', () => ({
  __esModule: true,
  useApiGlobalState: () => ({}),
  useConfig: () => ({})
}));
