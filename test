// mock react-router-dom's Link so we don't need a Router in unit tests
jest.mock('react-router-dom', () => ({
  __esModule: true,
  // keep anything else your app might import (Navigate, useNavigate, etc.)
  ...jest.requireActual('react-router-dom'),
  Link: ({ children, ...props }: any) => <a {...props}>{children}</a>,
}));
