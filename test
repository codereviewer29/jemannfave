/**
 * Stub ESM module "crypto-js" so Jest doesn't try to parse it.
 * Must be before other imports so the mock is hoisted first.
 */
jest.mock('crypto-js', () => ({
  __esModule: true,
  default: {},                 // some code `import default` — keep it harmless
  SHA256: jest.fn((x: any) => String(x)),
  HmacSHA256: jest.fn(() => 'stub-hmac'),
  enc: { Utf8: {} },
}));
