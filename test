// Keep all real exports from @mui/material/styles, override only useTheme
jest.mock('@mui/material/styles', () => {
  const actual = jest.requireActual('@mui/material/styles');
  return {
    ...actual,
    useTheme: () => ({ direction: 'ltr' }), // whatever your component needs
  };
});
